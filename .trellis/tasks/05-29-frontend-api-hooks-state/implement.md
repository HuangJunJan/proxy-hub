# Implementation Plan: Frontend API Hooks State Cleanup

## Checklist
1. Add a small `useAsyncAction` hook.
2. Apply it to a high-value page action where duplicate loading state exists.
3. Build with `pnpm --dir web build`.
4. Commit changes.
